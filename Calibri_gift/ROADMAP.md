# Roadmap — сайт «Колибри» (Calibri_gift)

Имиджевый новогодний лендинг для ТК «Колибри»: корпоративные подарки как забота
о сотрудниках. Купить с сайта нельзя — цель: захотеть каталог → личный контакт/бот.

**Стек:** Next.js 15 + TypeScript + Tailwind v4 + Motion (framer-motion)
**Ветка:** `dev` · **Заявки:** почта + Google Таблица · **ЦА:** HR, собственники,
директора, закупки, главбухи, профсоюзы (компании от ~300 сотрудников)

**Палитра:** тёмно-синий `#0E1526` / бордовый `#7A2438` / золото `#E8B968` /
тёплый белый `#F7F3EC` · **Шрифты:** Playfair Display + Manrope

---

## ✅ Сделано

- [x] Репозиторий, ветка `dev`, структура монорепо (папка на сайт)
- [x] Скиллы: design-themes, framer-motion + установлен ui-ux-pro-max
- [x] Каркас Next.js 15 + Tailwind v4 + Motion, дизайн-токены
- [x] **Hero**: колибри из света (SVG path-draw + свечение + искры),
      canvas-снег 3 слоя с параллаксом от мыши, построчный reveal заголовка,
      эффект «свечи» на слове «заботу», магнитная CTA
- [x] **Scroll-storytelling**: pinned-сцена — подарок развязывается и
      открывается скроллом, тёплый свет, карточки-смыслы
      (Сотрудникам / Детям сотрудников / Руководителю)
- [x] Счётчики со свечением (11 лет / 1000+ компаний / 300+)
- [x] «Как рождается забота» — 3 шага до персонального КП
- [x] Тизер каталога без цен («Каталог — по личному запросу»)
- [x] Форма заявки (API-заглушка `/api/lead` с логированием)
- [x] Адаптив проверен: 375px / 1280px, без горизонтального скролла
- [x] Фиксы: переменные next/font на `<html>` (Tailwind v4),
      кастомный `useSceneProgress` вместо `useScroll` (баг sticky-target)
- [x] **Фотореалистичный подарок**: 9 кадров сгенерированы в Nano Banana 2
      (исходники: `design/gift-src/`, промпты: `design/nano-banana-prompts.md`),
      сконвертированы в WebP (`scripts/convert-gift.mjs`, 18 МБ → ~470 КБ),
      GiftScene переведён на кросс-фейд кадров 1–8 по скроллу,
      кадр 9 → OG-превью (`public/gift/og.jpg`)

- [x] Плавные склейки секвенции: useSpring-инерция, «наезд камеры» на кадрах,
      широкие диссолвы (сумма непрозрачностей = 1), снег и свечение поверх
- [x] Сторителлинг: 3 строки о заботе по мере открытия подарка
- [x] README: инструкция запуска для показа заказчику
- [x] Форма → Google Таблица + письмо менеджеру (Apps Script webhook,
      `LEAD_WEBHOOK_URL`, инструкция: `docs/lead-setup.md`)
- [x] Гирлянда-прогресс у правого края (десктоп)
- [x] Favicon-колибри (`app/icon.svg`), Schema.org Organization
- [x] Страница `/bot` — выбор Telegram / MAX (`NEXT_PUBLIC_TG_BOT_URL`,
      `NEXT_PUBLIC_MAX_BOT_URL`, вход по QR из каталога, noindex)

- [x] Снежный след курсора (десктоп, reduced-motion учтён)
- [x] Hero-фон: кадр 9 как тёплая текстура глубины (opacity 0.2 + градиент)
- [x] Страница-заготовка `/merch` (noindex, ссылка в footer)
- [x] `prefers-reduced-motion` — аудит: Snow/CursorSnow выключаются,
      GiftScene без инерции, колибри и индикатор скролла статичны
- [x] sitemap.xml + robots.txt (`/api/` закрыт)
- [x] Секции «Забота — это ещё и надёжность» (6 принципов, юрлица/ИП,
      декларация о соответствии) и «Доставка» (ТК, бесплатно в 5 регионов) —
      по материалам старого сайта заказчика
- [x] Футер: реальные контакты, соцсети (WhatsApp/Telegram/VK), ИНН/ОГРН
- [x] Форма-квиз на сайте полностью повторяет бота: 5 вопросов кнопками +
      город + дата (кнопки/своя) + особое пожелание + контакты; единая
      таблица (лист «Заявки», источник «сайт»/«бот»), один Apps Script.
      API-роут `/api/lead` прокидывает все поля (город/дата/пожелание)
- [x] Бот: код готов (`bot/Code.gs`, локально, вне git) — анкета кнопками,
      прогресс, запись в таблицу, письмо менеджеру; @kolibri_care_bot
- [x] **Видео-сцена Veo 3**: два клипа 1080p → 140 кадров WebP (5.4 МБ,
      `scripts/extract-scene.mjs`: склейка, кроп вотермарка, 8.75 к/с) →
      канвас-секвенция в GiftScene (техника Apple), прогрессивная загрузка
      (каркас каждый 7-й кадр → фон), постер до первого кадра
- [x] **Публикация на GitHub Pages**: https://hoxitoo.github.io/WebSites/
      (статический экспорт, workflow deploy-pages.yml, форма шлёт в Apps
      Script напрямую). Инструкция по Vercel — в README
- [x] Ветка по умолчанию репозитория переведена на `main`

- [x] **Правки заказчицы (июль 2026)**: новый hero-текст («знак заботы
      и признания… Индивидуальный подход — в основе всего»), секция
      «А вот и сама радость» — ёлочные шары с настоящими фото детей
      (кадры из презентации), «Нам доверяют» (ЛУКОЙЛ, Росатом, Россети,
      ТАНТК, Роспотребнадзор…) + благотворительность, доставка по данным
      презентации NEW (ЮФО/Крым от 50 000 ₽, евро-часть РФ от 100 000 ₽,
      свой автопарк, фиксированная дата), векторный логотип в футере
      (плашка), адрес Сормовская 1/2

- [x] **Дополнение текстов заказчицы (июль 2026)**: усилены «Цифры»
      (11 лет / 99,9% отгрузок в срок / 1000+ клиентов, 85% >9 лет),
      «Как это работает» переписан под реальный процесс (чат-бот →
      3 линейки → фиксируем без изменений наполнения), новый блок
      «Декабрь без лишней суеты» (3 вместо 200 / только выполнимое /
      до 95% времени), «Надёжность» пересобрана (фикс цены+даты,
      персональный менеджер, единое решение для филиалов, подарок как
      сообщение, 100+ городов), вторичный CTA «Созвонимся — 15 минут»

## 🔜 Осталось — сайт

- [ ] **Открывается ИМЕННО коробка заказчицы** (её брендовые боксы: зелёный
      6-гранник с барашками/детьми и красный с бантом). Нужны hi-res файлы
      боксов. Два пути: (A) новый Veo-клип с её коробкой как реф-кадр —
      промпт готов в `design/veo-client-box-prompt.md`, риск — печатная
      графика (барашки/дети/текст) может «плыть» в движении; (B) открытие
      коробки в коде из плоских изображений — графика остаётся чёткой,
      но «магия света» скромнее. Ждём файлы + выбор пути
- [ ] Добавить секрет `LEAD_WEBHOOK_URL` в GitHub (Settings → Secrets) —
      чтобы форма на Pages писала в таблицу
- [ ] Свой домен + прод-хостинг (Vercel Pro / хостинг с Node.js)
- [ ] Детальный список правок заказчицы после смотра с коллегами (ждём)

## Бот v5 (июль 2026) — КП в чат + инструкции заказчице ✅

- [x] `/myid` — менеджер узнаёт свой Telegram ID (для белого списка)
- [x] Отправка КП менеджером в чат клиента: `/kp <ID>` → следующим
      сообщением текст/фото/файл → уходит клиенту от имени Службы заботы
      (только для `CONFIG.MANAGER_IDS`)
- [x] Пошаговые инструкции для нетех-заказчицы (папка `bot/`, вне git):
      `Заказчице_0_Начните_здесь`, `_1_Таблица`, `_2_Скрипт`,
      `_3_Бот_в_MAX`, `_4_Менеджеры_и_КП`
- [ ] Внести реальные ID менеджеров в `CONFIG.MANAGER_IDS` (ждём от заказчицы)
- [x] Вопросы анкеты: город доставки + дата (кнопки/текст) → 2 новые
      колонки в таблице «Город», «Дата поставки» (итого 15)
- [x] QR на бота (Telegram) внизу сайта — секция BotQr, `public/bot-qr.png`
- [x] Kling-промпт открытия РЕАЛЬНОЙ коробки заказчицы
      (`design/kling-client-box-prompt.md`) — под её зелёный 6-гранник
      (кадры `design/brand/box-scene-1/2.jpg`)
- [x] **Сцена открытия = РЕАЛЬНАЯ коробка заказчицы** (Kling): клип 1 бант
      развязывается + клип 2 крышка открывается/свет/вылет шаров → 140 кадров
      (`scripts/extract-scene.mjs`: склейка, срез вотермарка Kling, 14 к/с).
      Старые Veo-видео сохранены как `design/gift-src/video/legacy 1|2.mp4`.
      Видео-исходники убраны из git (тяжёлые) — в репо только кадры
- [x] Адаптив/производительность: сцена подарка на телефоне не обрезается
      (canvas contain на узких экранах, cover на ~16:9); hero на 100svh;
      снег — вдвое меньше частиц на мобильных + пауза rAF вне экрана
- [x] Hero: овечка с подарком — бледная подложка под текстом (opacity 0.12,
      `hero-sheep.webp`), колибри возвращена в правую колонку; OG-превью
      пересоздано из зелёной коробки
- [x] Сцена подарка: коробка меньше (contain ×0.9, воздух по краям),
      края растушёваны в фон того же тона (SCENE_BG) — бесшовно, без резкого
      прямоугольника и расхождения фонов; убран дорогой blur (лагал ПК);
      кадры перевыгружены 1700px (чётче)
- [x] Текст над коробкой держится дольше (шире окна storyLines)
- [x] Плавность скролла: `img.decode()` заранее (нет подвисаний на смене
      кадра), DPR-кап 1.5 на мобильных, снег вне экрана на паузе + реже
- [x] Правки ритма сцены: обрезано статичное начало (−1.6с), меньше кадров
      (101 вместо 140), видео укладывается в первые 72% скролла, карточки
      выходят ПОСЛЕ полного вылета шаров (не перекрывают их)
- [x] Секция «А вот и сама радость» переведена на РЕАЛЬНЫЕ фирменные шары
      заказчицы (5 стеклянных шаров с барашками/детьми, фон убран,
      `public/gift/baubles/`) вместо кропов детей

## 🔜 Осталось — бот в MAX (новое требование)

- [ ] Второй бот в мессенджере MAX, данные → та же Google Таблица
      (источник «макс»). Логика анкеты/запись в лист переиспользуются;
      нужен адаптер под MAX Bot API (другие эндпоинты и формат клавиатур).
      Оценка: ~1 день + тесты; главный вопрос — паритет фич MAX API
      (инлайн-кнопки/callback). Кнопка MAX на странице `/bot` уже есть.
      Инструкция создания бота для заказчицы готова (`Заказчице_3_Бот_в_MAX`)

## Бот «Служба заботы Деда Мороза» — ✅ работает (v3)

- [x] @kolibri_care_bot зарегистрирован, страница `/bot` ссылается на него
- [x] Архитектура: Telegram → Cloudflare Worker (мгновенный 200) →
      Apps Script (логика) → Google Таблица + письмо менеджеру.
      Прямой webhook на Apps Script не годится: 302 → ретраи → минуты задержки
- [x] Анкета: 5 вопросов кнопками с прогрессом → компания → почта →
      телефон (можно пропустить) → «особое пожелание» (можно пропустить)
- [x] Постоянная кнопка «🎁 Начать заново», дедупликация update_id,
      DEBUG-режим (ошибки в чат), helper file_id для приветственных медиа
- [x] Проверено сквозно: заявка в таблице, письмо приходит
- [x] Почта: принимает любые домены (mail.ru, yandex.ru, gmail…),
      извлекает адрес из любого сообщения (`extractEmail`)
- [x] **Душевность v4**: обращение по имени (с проверкой — «странные»
      имена отсекаются), живые реакции на каждый ответ, тёплый финал
      + благодарность за «особое пожелание»
- [x] Фикс: телефон «+7…» ломал ячейку Google Sheets (#ERROR — Sheets
      считал формулой). `safeCell` экранирует `= + - @`, заодно защита
      от инъекций формул из формы
- [ ] Приветственные фото/видео от основателя — вставить file_id в CONFIG
      (голос/лицо — главный источник тепла)
- [ ] Оформление в BotFather по `bot/BRANDING.md` (описание, аватар)
- [ ] Отправка КП менеджером прямо в чат клиента (обсуждается, см. ниже)
- [ ] «Печатает…» перед ответами бота (опционально, добавляет живости)
- [ ] `DEBUG: false` перед боевым запуском
- [ ] Правки заказчицы после показа коллегам (ждём список)
- [ ] Передача заказчице по `bot/HANDOVER.md` (в т.ч. Revoke токена!)

## 🤝 Отправка КП менеджером в чат — да, реализуемо

Менеджер пишет боту команду с chatId клиента (он есть в таблице) +
прикрепляет файл КП / текст → бот пересылает это клиенту в его чат.
Реализация после согласования формата.

## 📌 Решения и договорённости

- Сайт только под новогоднюю кампанию (мерч — второстепенно, позже)
- Цены и каталог НЕ публикуем — защита идей от конкурентов
- Тон: тёплый-эмоциональный, премиальный (не мультяшный) —
  «руководитель дарит не подарок, а признание вклада»
- Пуш в GitHub — по команде заказчика разработки
