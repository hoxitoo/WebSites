node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts
.env*.local

# бот с токеном — только локально, не для публичного репо
bot/

# видео-исходники сцены (тяжёлые, сайту не нужны — нужны только кадры public/gift/seq)
design/gift-src/video/
