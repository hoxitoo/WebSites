node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts
.env*.local

# Боты (Telegram + MAX) с токенами — только локально, не для публичного репо.
# Косая черта в начале обязательна: без неё правило заглушало бы и app/bot/ —
# страницу сайта с выбором мессенджера.
/bot/

# страховка: файлы с ключами не должны попасть в репо ни при каком имени
*token*.txt
*.token

# видео-исходники сцены (тяжёлые, сайту не нужны — нужны только кадры public/gift/seq)
design/gift-src/video/

# оригиналы логотипов поставщиков (5 МБ). Лежат локально, чтобы
# scripts/make-suppliers.mjs можно было перезапустить без архива
# от заказчицы; сайту нужны только готовые public/brand/factory-*.webp
design/suppliers/

# внутренние рабочие файлы (не для публичного репо)
ПРАВКИ-СТАТУС.md
