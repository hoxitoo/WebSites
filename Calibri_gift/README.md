# Сайт «Колибри» — как запустить для показа

Имиджевый новогодний лендинг ТК «Колибри». Ниже — как поднять сайт на своём
компьютере и показать заказчику. Ничего, кроме Node.js, не нужно.

## Требования (один раз)

1. Установить **Node.js 20 LTS или новее**: https://nodejs.org (кнопка "LTS").
   Проверить в терминале: `node -v` → должно показать v20.x или выше.
2. Получить код:
   ```bash
   git clone https://github.com/hoxitoo/WebSites.git
   cd WebSites/Calibri_gift
   npm install
   ```
   (если код уже есть на диске — просто `cd` в папку `Calibri_gift`)

## Показ заказчику — продакшен-режим (рекомендуется)

Быстрый, без дев-оверлеев, максимально плавные анимации:

```bash
npm run build
npm run start
```

Открыть **http://localhost:3000** — готово. Остановить сервер: `Ctrl+C`.

> `npm run build` нужно повторять после каждого изменения кода.
> Для разработки удобнее dev-режим (см. ниже) — там всё обновляется само.

## Показать на телефоне (та же Wi-Fi сеть)

```bash
npm run start -- -H 0.0.0.0
```

Узнать IP компьютера: `ipconfig` (Windows) → строка IPv4, например `192.168.1.42`.
На телефоне открыть `http://192.168.1.42:3000`.
Скролл-сцену с подарком обязательно смотреть и на телефоне!

## Показать по ссылке (заказчик не рядом) — ngrok

1. Запустить сайт в прод-режиме (первый терминал):
   ```bash
   npm run build
   npm run start
   ```
2. Во втором терминале поднять туннель:
   ```bash
   ngrok http 3000
   ```
3. Скопировать из вывода ngrok ссылку `Forwarding` вида
   `https://xxxx-xx-xx.ngrok-free.app` — отправить заказчику.

Нюансы:
- Ссылка живёт, пока работают **оба** терминала (сервер + ngrok).
- На бесплатном тарифе ngrok при каждом запуске даёт новую ссылку, а перед
  сайтом показывает страницу-предупреждение с кнопкой «Visit Site» —
  предупредить заказчика, что это нормально, нажать кнопку один раз.
- Постоянный адрес (без смены ссылки): в личном кабинете ngrok создать
  бесплатный static domain и запускать
  `ngrok http 3000 --url=ваш-домен.ngrok-free.app`.
- ngrok не установлен/не залогинен: https://ngrok.com/download, затем
  `ngrok config add-authtoken <токен из кабинета>`.

Запасной вариант без ngrok: `npx localtunnel --port 3000`.
Постоянная демо-ссылка: деплой на Vercel (`npm i -g vercel && vercel`).

## Постоянная демо-ссылка — GitHub Pages

Сайт автоматически публикуется на **https://hoxitoo.github.io/WebSites/**
при каждом пуше в `main` (workflow `.github/workflows/deploy-pages.yml`).

Разовая настройка в репозитории на GitHub:
1. **Settings → Pages → Build and deployment → Source: «GitHub Actions»**.
2. Чтобы форма-квиз слала заявки в Google Таблицу:
   **Settings → Secrets and variables → Actions → New repository secret** →
   имя `LEAD_WEBHOOK_URL`, значение — URL Apps Script (`…/exec`).
   Без секрета сайт работает, но отправка формы вернёт ошибку.
3. Запустить вручную: вкладка **Actions → Deploy to GitHub Pages → Run workflow**.

Особенности Pages-версии: статический хостинг — серверного `/api` нет,
форма шлёт заявку в Apps Script напрямую из браузера.

## Режим разработки

```bash
npm run dev
```

http://localhost:3000, изменения кода подхватываются на лету.

## Куда падают заявки

Квиз на сайте и бот @kolibri_care_bot собирают одну анкету (5 вопросов)
в один лист «Заявки» одной Google Таблицы; различие — колонка «Источник»
(`сайт` / `бот`).

- Без настройки: заявки с сайта пишутся в консоль сервера (режим разработки).
- Боевой режим: один Google Apps Script на оба канала — обзор в
  [docs/lead-setup.md](docs/lead-setup.md), код и пошаговая установка —
  в локальной папке `bot/` (в git не попадает: содержит токен бота).
  После деплоя скрипта URL указывается в `.env.local` → `LEAD_WEBHOOK_URL`
  (пример — в `.env.example`).

## Где что менять

| Что | Файл |
|-----|------|
| Тексты hero, заголовок | `components/Hero.tsx` |
| Сцена с подарком, сторителлинг-строки | `components/GiftScene.tsx` |
| Цифры, «Как это работает», тизер каталога | `components/Sections.tsx` |
| «Надёжность», «Доставка» | `components/About.tsx` |
| Квиз-форма заявки (вопросы/варианты) | `components/LeadForm.tsx` |
| Страница выбора мессенджера бота | `app/bot/page.tsx` |
| Тексты бота (вопросы, приветствие) | `bot/Code.gs` (локально) |
| Цвета и шрифты | `app/globals.css` |
| Контакты, соцсети, реквизиты | `components/Footer.tsx` |

## Видео-сцена с подарком

Сцена открытия подарка — канвас-секвенция из 140 WebP-кадров
(`public/gift/seq/`), нарезанных из двух клипов Veo 3.

Чтобы заменить видео: положи новые клипы как
`design/gift-src/video/new 1.mp4` и `new 2.mp4` (1080p, статичная камера,
8 сек каждый) и запусти:

```bash
node scripts/extract-scene.mjs
```

Скрипт склеит клипы, срежет вотермарк (кроп правых 200px) и нарежет кадры.
Hero-фон и OG-превью пересобираются отдельно: `node scripts/convert-gift.mjs`
(исходники-картинки в `design/gift-src/`).

План работ и статус: [ROADMAP.md](ROADMAP.md)
